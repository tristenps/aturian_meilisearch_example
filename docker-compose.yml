version: "3"
services:
  web:
    build: .
    ports:
      - "80:3100"
    depends_on:
      - "meili"
    command: []

  meili:
    image: getmeili/meilisearch:latest
    ports:
      - "81:7700"
    volumes:
      - ./data.ms:/data.ms
